[package]
name = "broken-app"
version = "0.1.0"
edition = "2024"

[dependencies]
blondie = "0.5.3"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "criterion"
harness = false

[profile.release]
opt-level = 0 
debug = true
strip = false